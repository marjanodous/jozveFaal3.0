<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jozvefaal30">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JozveFaal30">
        <activity android:name=".tab_menu"></activity>
        <activity android:name=".tozihat" />
        <activity android:name=".support" />
        <activity android:name=".student_profile" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".signup"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".slide_sign_up2" />
        <activity android:name=".slide_sign_up1" />
        <activity
            android:name=".login"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".about" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>